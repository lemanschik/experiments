/**
 * Core resolver
 */
 ```ts
 // Compatability layer for external modules. 
 const modules = { "module::file:///home/stealify/me.js": ()=> 1{ } };
 
 const Resolver = (specifier, component) => {
  return "loadableSpecifier for the host."
 }
 
 ```
